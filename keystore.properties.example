# Configuración del Keystore para firmar APKs de Release
# Copia este archivo a keystore.properties y completa los valores
# IMPORTANTE: keystore.properties está en .gitignore y NO debe subirse a GitHub

# Ruta al archivo keystore (relativa al directorio raíz del proyecto)
storeFile=app/keystore.jks

# Contraseña del keystore
storePassword=tu_contraseña_del_keystore

# Alias de la clave
keyAlias=key0

# Contraseña de la clave
keyPassword=tu_contraseña_de_la_clave

# INSTRUCCIONES PARA CREAR EL KEYSTORE:
# 
# 1. Abre una terminal en el directorio raíz del proyecto
# 2. Ejecuta el siguiente comando (reemplaza los valores entre <>):
#
#    keytool -genkey -v -keystore app/keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias key0
#
# 3. Completa la información solicitada
# 4. Copia este archivo a keystore.properties y completa los valores
# 5. ¡IMPORTANTE! Guarda una copia segura del keystore.jks y las contraseñas
#    Si pierdes el keystore, NO podrás actualizar la app en Google Play Store

